# SatTrack — required environment variables
# Copy to .env and fill in real values (never commit .env)

SUPABASE_URL=https://mwmbcfcvnkwegrjlauis.supabase.co
SUPABASE_SERVICE_KEY=          # Dashboard → Settings → API → service_role key
SUPABASE_JWT_SECRET=           # Dashboard → Settings → API → JWT Settings → JWT Secret
ADMIN_SECRET=                  # choose a strong random string for /v1/admin/* protection
